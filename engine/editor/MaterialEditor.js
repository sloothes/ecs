
//	Material Editor.

//	Create a Material to hold editor values.
	const materialEditor = (function( editor ){















































		return editor;

	})( new THREE.Material() );
